name: ai_flutter_app_factory
version: 1.2.2
description: Foundation code for the AI Flutter App Factory.
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.25.0
